# Telegram Bot для управления данными сайта

Telegram-бот на aiogram 3.x для управления данными персонального сайта. Бот предоставляет интерфейс для добавления, редактирования и удаления информации о кофе, книгах, винилах и других коллекциях.

## Особенности

- ✅ **Безопасность**: Доступ только для администратора по Telegram ID
- ✅ **FSM**: Использует конечные автоматы для всех CRUD-сценариев
- ✅ **Service Layer**: Переиспользует существующие репозитории из backend
- ✅ **Docker**: Готов к запуску в контейнере
- ✅ **Логирование**: Подробное логирование всех операций
- ✅ **Типизация**: Полная типизация кода

## Структура проекта

```
telegram/
├── bot.py                 # Точка входа
├── config.py             # Конфигурация
├── database.py           # Подключение к БД
├── requirements.txt      # Зависимости
├── Dockerfile           # Docker образ
├── handlers/            # Обработчики команд
│   ├── start.py        # /start и основное меню
│   └── coffee.py       # Управление кофе
├── states/             # FSM состояния
│   └── coffee_states.py
├── keyboards/          # Клавиатуры
│   └── coffee_keyboards.py
└── services/           # Бизнес-логика
    └── coffee_service.py
```

## Настройка

### 1. Создание бота

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Получите токен бота
4. Узнайте свой Telegram ID (можно через [@userinfobot](https://t.me/userinfobot))

### 2. Переменные окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
# Обязательные переменные
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_TELEGRAM_ID=your_telegram_user_id

# База данных (те же что и для backend)
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=your_db_name

# Опциональные
LOG_LEVEL=INFO
```

### 3. Запуск

#### С Docker Compose (рекомендуется)

```bash
# Из корневой директории проекта
docker-compose up -d telegram-bot
```

#### Локально для разработки

```bash
cd telegram/
pip install -r requirements.txt
python run_local.py  # Использует локальный скрипт с правильными путями
```

**Альтернативно:**
```bash
# Из корневой директории проекта
cd my_easy_site/
PYTHONPATH=backend python telegram/bot.py
```

## Использование

### Команды

- `/start` - Запуск бота и главное меню
- `/help` - Справка по командам

### Функциональность

#### ☕ Управление кофе

**Бренды:**
- Просмотр списка брендов
- Добавление нового бренда
- Редактирование бренда
- Удаление бренда

**Кофе:**
- Просмотр списка кофе
- Добавление нового кофе (с выбором бренда)
- Указание региона и способа обработки
- Редактирование информации о кофе
- Удаление кофе

**Отзывы:**
- Добавление отзывов на кофе
- Выбор способа приготовления
- Оценка по 10-балльной шкале
- Текстовые заметки
- Редактирование и удаление отзывов

### Безопасность

- Доступ только для пользователя с указанным `ADMIN_TELEGRAM_ID`
- Все остальные пользователи получают сообщение об отказе в доступе
- Логирование всех попыток доступа

## Архитектура

### Service Layer

Бот использует существующие репозитории из backend через service layer:

```python
# Пример использования
async with get_db_session() as db:
    service = CoffeeService(db)
    coffee = await service.create_coffee(
        brand_id=brand_id,
        name=name,
        region=region
    )
    await service.commit()
```

### FSM (Finite State Machine)

Все CRUD-операции используют состояния для пошагового ввода данных:

```python
class CoffeeStates(StatesGroup):
    waiting_for_brand_selection = State()
    waiting_for_coffee_name = State()
    waiting_for_coffee_region = State()
    # ...
```

### Обработка ошибок

- Валидация пользовательского ввода
- Обработка ошибок базы данных
- Логирование всех ошибок
- Понятные сообщения пользователю

## Мониторинг

### Логи

Бот ведет подробные логи:

```
2024-01-01 12:00:00 - INFO - 🚀 Запуск Telegram-бота...
2024-01-01 12:00:01 - INFO - ✅ Подключение к базе данных успешно
2024-01-01 12:00:02 - INFO - 🤖 Бот запущен: @your_bot (Your Bot Name)
```

### Health Check

Docker Compose включает health check для мониторинга состояния бота.

## Разработка

### Добавление новых функций

1. Создайте новые состояния в `states/`
2. Добавьте клавиатуры в `keyboards/`
3. Реализуйте обработчики в `handlers/`
4. При необходимости создайте сервисы в `services/`

### Тестирование

```bash
# Запуск в режиме разработки
LOG_LEVEL=DEBUG python bot.py
```

## Troubleshooting

### Частые проблемы

1. **ModuleNotFoundError: No module named 'app'**
   - Убедитесь, что используете правильный контекст сборки Docker
   - Для локальной разработки используйте `python run_local.py`
   - Или установите PYTHONPATH: `PYTHONPATH=backend python telegram/bot.py`

2. **Бот не отвечает**
   - Проверьте правильность `BOT_TOKEN`
   - Убедитесь, что бот запущен

3. **Доступ запрещен**
   - Проверьте `ADMIN_TELEGRAM_ID`
   - Узнайте свой ID через [@userinfobot](https://t.me/userinfobot)

4. **Ошибки базы данных**
   - Проверьте подключение к PostgreSQL
   - Убедитесь, что миграции выполнены
   - Проверьте переменные POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB

### Логи

Проверьте логи контейнера:

```bash
docker-compose logs telegram-bot
```

## Roadmap

- [ ] Управление книгами
- [ ] Управление винилами
- [ ] Управление проектами
- [ ] Статистика и аналитика
- [ ] Экспорт данных
- [ ] Backup функции